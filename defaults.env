# Default environment variables for Architecture Evaluation Tool
#
# IMPORTANT: Before running the application, copy this file to .env:
#   cp defaults.env .env
#
# Then edit .env to customize settings for your local environment.
# The .env file is gitignored and should not be committed.
#
# For Docker: docker-compose will automatically load variables from .env

# Application Settings
APP_NAME=Architecture Evaluation Tool API
APP_VERSION=0.1.0

# Database Configuration
# Override DATABASE_URL for production deployments
DATABASE_URL=postgresql://arch_eval:arch_eval_password@localhost:5432/arch_eval_db

# CORS Settings
CORS_ALLOW_ORIGINS=["*"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]

# Storage Configuration
STORAGE_ROOT=storage

# Telemetry Settings (disabled by default)
# Set to true to enable telemetry data collection
TELEMETRY_ENABLED=false

# Individual telemetry components
TELEMETRY_TRACES_ENABLED=false
TELEMETRY_METRICS_ENABLED=false
TELEMETRY_LOGS_ENABLED=false

# Telemetry service information
TELEMETRY_SERVICE_NAME=architecture-evaluation-tool
TELEMETRY_SERVICE_NAMESPACE=backend

# OTLP endpoint configuration
# To get Grafana Cloud credentials:
# 1. Sign up at https://grafana.com/auth/sign-up/create-user
# 2. Go to Connections → Add new connection → OpenTelemetry
# 3. Copy OTLP endpoint (format: https://otlp-gateway-prod-<region>-<number>.grafana.net/otlp)
# 4. Create API token: Administration → API Keys → New API Key (Role: MetricsPublisher, TracesPublisher, LogsPublisher)
# 5. Base64 encode: echo -n "INSTANCE_ID:API_TOKEN" | base64
# 6. Use format: Authorization=Basic%20<base64-encoded> (URL-encoded)
#    Or JSON format: {"Authorization": "Basic <base64-encoded>"}
# Configure for Grafana Cloud or other OTLP-compatible backends
TELEMETRY_OTLP_ENDPOINT=https://otlp-gateway-prod-<region>-<number>.grafana.net/otlp
TELEMETRY_OTLP_HEADERS=Authorization=Basic%20<your-base64-encoded-api-key>
TELEMETRY_OTLP_INSECURE=false

