# Architecture Evaluation Tool – Backend

FastAPI backend configured with a layered (DDD-inspired) structure.

## Quick start

```bash
poetry install
poetry run uvicorn main:app --reload
```

## Tests

```bash
poetry run pytest
```
